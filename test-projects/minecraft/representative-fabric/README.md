# Representative Fabric

A Fabric project inspired by the Mod Menu build structure. It includes a dedicated Maven
repository, a compile-only mod dependency, resource version expansion, a sources JAR, and Maven
publishing configuration.

It retains only the build structure and a small amount of code; it does not copy Mod Menu source
code or assets.
